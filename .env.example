# Local only. The plugin reads TYPESAFE_API_KEY from the process
# environment or from DSH credentials under the same name.
TYPESAFE_API_KEY=

# Used only by `pnpm run live`. Point it at a file that holds
# TYPESAFE_API_KEY=<key>.
TYPESAFE_KEY_FILE=
